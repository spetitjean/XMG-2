%token id
%token int
%token string 
%token bool

%type Value Value

%ext Else Else

%%

Value : id {$$=$1} 
      | '?' id {$$=var($2)} 
      | '!' id {$$=const($2)}
      | bool {$$=$1}
      | int {$$=$1}
      | string {$$=$1}
      | Else {$$=$1};

%%